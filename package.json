{
  "name": "visual-financial-simulator",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p apps/web/tsconfig.json --noEmit",
    "check": "pnpm typecheck && pnpm test",
    "dev": "pnpm --filter @fin/web dev",
    "build": "pnpm --filter @fin/web build"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
